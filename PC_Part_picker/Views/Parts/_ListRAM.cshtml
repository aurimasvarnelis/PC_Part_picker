@model PC_Part_picker.ViewModels.PartViewModel

<p>
    <a asp-action="CreateRam">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ram.First().Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ram.First().Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ram.First().Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ram.First().Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ram.First().Manufacturer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ram.First().Color)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Ram)
        { 
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Model)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Rating)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Manufacturer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Color)
                </td>
                <td>
                    <a asp-action="EditRam" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="DetailsRam" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="DeleteRam" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
