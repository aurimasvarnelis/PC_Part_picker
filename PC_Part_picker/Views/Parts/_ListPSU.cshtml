@model PC_Part_picker.ViewModels.PartViewModel

<p>
    <a asp-action="CreatePsu">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Psu.First().Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Psu.First().Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Psu.First().Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Psu.First().Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Psu.First().Manufacturer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Psu.First().Color)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Psu)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Model)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Rating)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Manufacturer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Color)
                </td>
                <td>
                    <a asp-action="EditPsu" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="DetailsPsu" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="DeletePsu" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
