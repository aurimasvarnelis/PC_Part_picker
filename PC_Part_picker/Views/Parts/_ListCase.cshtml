@model PC_Part_picker.ViewModels.PartViewModel

<p>
    <a asp-action="CreateCase">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Case.First().Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Case.First().Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Case.First().Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Case.First().Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Case.First().Manufacturer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Case.First().Color)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Case)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Model)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Rating)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Manufacturer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Color)
                </td>
                <td>
                    <a asp-action="EditCase" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="DetailsCase" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="DeleteCase" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
